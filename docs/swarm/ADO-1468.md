# ADO-1468

## Goal

Create `SECURITY.md` for `xrmghost-skills` and add a `README.md` link to the repository security policy.

## Scope and changes

- Added root `SECURITY.md` with the required H2 sections:
  - `Capability Profile`
  - `Data Handling`
  - `Supported Versions`
  - `Reporting a Vulnerability`
- Documented the actual repository security posture: `xg` CLI usage, local scenario JSON authoring, no arbitrary shell, no direct network access from the skills, and no secret handling by the skills.
- Documented `--data-source auto|live` as explicit opt-in, Pro-gated, and read-only, including local cache behavior.
- Added a `README.md` link to `SECURITY.md` in the documentation section.

## Commands executed

1. `python3 - <<'PY' ...` — pretty-printed the task bundle to validate schema and scope. Exit 0.
2. `python3 - <<'PY' ...` — checked for `prior_review` findings in the bundle. Exit 0.
3. `git remote -v` — confirmed repository remote. Exit 0.
4. `git branch --show-current` — confirmed worktree branch. Exit 0.
5. `git status --short` — inspected working tree before edits. Exit 0.
6. `mkdir -p docs/swarm` — created evidence directory. Exit 0.
7. `test -f SECURITY.md && echo "OK: SECURITY.md presente" || echo "MISSING: SECURITY.md"` — validated file presence after edits. Exit 0.
8. `rg '^## (Capability Profile|Data Handling|Supported Versions|Reporting a Vulnerability)' SECURITY.md` — validated required H2 sections. Exit 0.
9. `rg -i 'SECURITY\.md' README.md` — validated README link. Exit 0.
10. `rg -i '(password|secret|token|endpoint|credential)' SECURITY.md && echo 'WARNING: possibile info sensibile' || echo 'OK: nessuna info sensibile rilevata'` — checked for sensitive terms. Exit 0.
11. `grep -E '^## (Capability Profile|Data Handling|Supported Versions|Reporting a Vulnerability)' SECURITY.md` — validated required H2 sections with the prompt-specified command style. Exit 0.
12. `grep -i 'SECURITY.md' README.md` — validated README link with the prompt-specified command style. Exit 0.
13. `grep -iE '(password|secret|token|endpoint|credential)' SECURITY.md && printf 'WARNING: possibile info sensibile\n' || printf 'OK: nessuna info sensibile rilevata\n'` — checked for sensitive terms with the prompt-specified command style. Exit 0.
14. `npm test -- --testPathPattern=security-md` — attempted required test command. Exit non-zero because the repository has no `package.json` or configured `test` script.
15. `git add -A` — staged scoped changes. Exit 0.
16. `git commit -m "Add repository security policy"` — created commit. Exit 0.
17. `git push origin swarm/ADO-1458/run-20260604T082211Z/ADO-1468` — pushed branch. Exit 0.

## Acceptance criteria verification

- `SECURITY.md` present in repo root: verified.
- All four required H2 sections present: verified.
- `README.md` contains a working `[Security Policy](SECURITY.md)` link: verified.
- No sensitive credentials or internal endpoints documented: verified.
- Content aligned with ADO-1460 findings and current skill behavior: verified against repository skill docs and live-data reference.

## Test results

- Repository validation commands passed.
- The requested `npm test -- --testPathPattern=security-md` command failed because this Markdown-only repository has no `package.json` and no configured `test` script. This did not block the scoped documentation change; the required content was validated with direct repository checks instead.

## Residual risks

- The response-time targets in `Reporting a Vulnerability` should be confirmed by the maintainer during review.
- Security posture remains documentation-based; if skill behavior changes later, `SECURITY.md` must be updated to stay accurate.

## Review notes

- Vulnerability disclosure contact uses the public maintainer email already present in repository commit metadata.
- No files under `skills/*/SKILL.md` or `.github/` were modified.
